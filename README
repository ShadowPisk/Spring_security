Для запуска данного проекта, необходимо:
  - загрзить необходимые зависимости из pom.xml;
  - подключиться к бд(в данном проекте испльзуется MySQL) 
      порт: 3306,
      имя пользователя: 'root'
      пароль: 'root';
  - прописать в бд CREATE SCHEMA `table_task` DEFAULT CHARACTER SET utf8 ;
  - через maven произвести запуск такой сигнатры "mvn clean package";
  - запустить проект через метод "main" класса "SpringSecurityApplication", для инициализации таблиц в бд;
  - запустить скрипт "script.sql", расположенного по адресу: "spring_security/src/main/resources/sql", для иницализации начальных данных в бд;
  - запустить браузер и ввести в адрессную строку пусть: "localhost:8080/";

данные для авторизации:
  - пользователь с ролью ROLE_ADMIN:
    логин: 'admin';
    пароль: 'admin';
  - пользователь с ролью ROLE_USER:
    логин: 'user';
    пароль: 'user';
